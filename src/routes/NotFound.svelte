<script lang="ts">
  // 404 fallback route.
</script>

<header>
  <h1>Not found</h1>
  <p>That page doesn't exist.</p>
</header>

<a class="back" href="#/today">Go to Today</a>

<style>
  .back {
    display: inline-flex;
    align-items: center;
    min-height: var(--tap-min);
    padding: 0 var(--s-4);
    border-radius: var(--r-pill);
    background: var(--accent-weak);
    color: var(--accent);
    font-weight: 600;
  }
</style>
