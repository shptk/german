/*
 * Content loading: manifest-first boot, lazy per-module fetch (memoized), and
 * zod validation at load. Content is immutable and shipped from public/content.
 *
 * Implemented in Milestone M3 (see DESIGN.md §2 and §3.4).
 */

export {};
